# Atalhos Makefile — suíte HTTP Dashboard 0.1

**Papel:** Engenheiro Implementador.
**Ordem:** adicionar run-test e clear-test ao Makefile da raiz.
**Baseline:** 12f8240, branch spec/dashboard-api-v1, árvore inicialmente limpa.

Adaptação de entrada da suíte 0.1, sem alterar cenários/contratos HTTP.
run-test chama run e exige TEST_API_URL. clear-test chama somente cleanup,
usando TEST_STATE; não é dependência nem etapa de run-test. Ambos usam
o manifesto padrão `.runs/dashboard.json`, configurável, sem apagá-lo.
Capability e timeout são opcionais; valores são transmitidos por ambiente e
argumentos shell entre aspas. Alvos são PHONY e mantêm o alvo padrão existente.
README e mapa atualizados. Os comandos compilam o runner quando necessário.

Verificações: make -n para ambos os alvos e parâmetros opcionais, incluindo
caminho com espaço; sintaxe dos scripts expandidos com /bin/sh -n; todos
exit 0. Integridade textual conferida. Nenhum dotnet run, teste, cleanup, HTTP,
banco ou build foi executado nesta adaptação. Código construível inalterado.
A conferência sintática não comprova execução funcional, que permanece
Not Executed. Workflow In Progress; entrega na branch corrente sem merge.
