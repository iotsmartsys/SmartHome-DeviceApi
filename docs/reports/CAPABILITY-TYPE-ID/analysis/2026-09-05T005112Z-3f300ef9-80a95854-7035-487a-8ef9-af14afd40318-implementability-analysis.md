# Análise de implementabilidade — CapabilityType por id 0.1

**Classe da fonte:** Relatório

**Papel:** Autor da Especificação e Engenheiro Analista, na mesma atuação autorizada

**Especificação:** `SHD-CAPABILITY-TYPE-ID-001@0.1`

**Revisão confrontada (SHA-256 do arquivo normativo):** `3f300ef99c15dca9c638e9342f1679126f5fb0d14c850bd1eb3883bf5069cbc5`

**Baseline Git:** `edf2cec663bb15ac5ce7e9f6cfcc268364c5a096`

**Execução:** `80a95854-7035-487a-8ef9-af14afd40318`

**Estado:** Final

## Problemas bloqueantes

Nenhum. A mudança é executável na arquitetura vigente. A entidade possui chave
inteira (`src/Core/Entities/CapabilityType.cs:7`), a criação já preenche o id
após INSERT (`src/Data.Repositories/Repositories/CapabilityTypeRepository.cs:28`)
e as mutações SQL já selecionam por id
(`src/Data.Repositories/Repositories/Queries/CapabilityTypeQuery.cs:61`,
`src/Data.Repositories/Repositories/Queries/CapabilityTypeQuery.cs:66`).
Os responsáveis materiais são controller/modelo HTTP de CapabilityType,
contrato do Core e repositório/queries correspondentes; não há necessidade de
capacidade arquitetural independente.

## Reconciliação anterior

Primeira análise desta especificação; nenhum bloqueador anterior a reconciliar.
O relatório de Groups pertence a outro contrato e não condiciona este recorte.

## Controle

Confrontados os oito requisitos, os cinco critérios, as bordas e as lacunas
`EKM-GAP-0001`, `EKM-GAP-0002` e `EKM-GAP-0003`; nenhum débito aceito relacionado.
Não há lacuna de cobertura. A inspeção localizou o controller como consumidor
funcional do contrato de repositório e os registros de DI existentes, sem
consumidor produtivo adicional que imponha ampliação do recorte.

Challenge limitado concluído: não foram encontradas contradição interna,
critério insatisfazível, remediação obrigatória fora do recorte ou disposição
anterior ausente. A rota separada de GET preserva nomes numéricos; a renomeação
é compatível com o UPDATE existente; o DELETE ausente mantém o status anterior.
O contrato de Groups exclui Capability Types de seu escopo
(`docs/specs/GROUPS-MAINTENANCE-SUPPORT.md:54`), sem conflito normativo.

## Restrições não bloqueantes

- PATCH/DELETE por nome deixam de ser o contrato público. Essa quebra é
  confirmada; alteração dos aplicativos consumidores fica fora do recorte.
- O POST atualmente perde a entidade que recebeu o id gerado e o PATCH repõe
  o nome da rota (`src/Api/Controllers/CapabilityTypeController.cs:45`,
  `src/Api/Controllers/CapabilityTypeController.cs:63`); ambos pertencem ao
  recorte, assim como preservar a identidade diante do novo campo HTTP.
- O schema completo não está versionado (`EKM-GAP-0002`). Nenhuma nova garantia
  de cascade foi introduzida; falhas de relacionamento continuam seguindo o
  middleware (`src/Api/Middlewares/ExceptionHandler.cs:49`).
- A análise por inspeção não certifica execução HTTP ou banco. Operações de
  validação funcional exigem autorização própria e não integram esta atuação;
  nenhum artefato de teste é contratado, e `tests/Api.Tests` permanece retirado.
- A guarda documental indicada em `AGENTS.md` não existe nesta baseline.
  Isso limita a verificação documental, sem impedir implementar o contrato.

**Classificação principal:** Pronta [`Ready`].
