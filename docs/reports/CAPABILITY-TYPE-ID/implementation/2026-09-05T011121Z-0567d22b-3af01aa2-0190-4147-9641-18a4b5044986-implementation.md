# Implementação — CapabilityType por id 0.2

**Classe da fonte:** Relatório

**Papel:** Engenheiro Implementador

**Especificação:** `SHD-CAPABILITY-TYPE-ID-001@0.2`

**Revisão autorizada e analisada Ready (SHA-256):** `0567d22b2b1a3b0a376893a6b52cf67e1d071309472bba89b70ec12b5042066d`

**Baseline:** `b310b01`, branch `spec/capability-type-id`, árvore inicialmente limpa.

**Execução:** `3af01aa2-0190-4147-9641-18a4b5044986`

**Estado:** Final — implementação concluída, encaminhada para Revisão.

## Autorização e execução

A ordem humana corrigiu o GET individual para somente
`api/v1/capabilities-types/:id`. A especificação foi emendada para 0.2 e
reanalisada antes da alteração do código, com relatório novo
`docs/reports/CAPABILITY-TYPE-ID/analysis/2026-09-05T011011Z-0567d22b-92cb7a87-7715-44ab-b007-9a3acecb5b82-implementability-analysis.md`.
A promoção foi registrada em `EKM-CHG-0004`. Após o build, somente o metadado
de implementação foi atualizado na especificação; o hash acima identifica o
snapshot analisado. Workflow permanece `In Progress`, sem integração.

## Mudança e inspeção

O controller conserva um único GET individual, com rota `{id:int}`, consulta
por id e respostas 200/404. O GET por nome foi removido e não existe alias
`id/{id}`. O POST usa a rota nomeada `GetCapabilityTypeById` e o id gerado pelo
banco para o Location (`src/Api/Controllers/CapabilityTypeController.cs:23`,
`src/Api/Controllers/CapabilityTypeController.cs:47`).

Foram inspecionados os oito requisitos e cinco critérios:

- CT-AC-001: listagem, filtro name, conversão com id e 204 vazio preservados.
- CT-AC-002: somente uma rota individual com restrição int e busca por chave;
  o retorno 404 para ausência e a leitura de ícones permanecem no fluxo existente.
- CT-AC-003: resposta após CreateAsync, id gerado no corpo e no Location da
  rota individual por id; não há mais geração de link por nome.
- CT-AC-004: PATCH preservado, inclusive id original após patch e renomeação;
  o GET por id continua lendo a entidade atualizada.
- CT-AC-005: DELETE por id, ausência 204 e transação permanecem intactos.

Resultado: conforme por inspeção; nenhum desvio identificado. Métodos internos
de repositório por nome permanecem disponíveis para usos existentes e não
expõem endpoint HTTP. Nenhuma alteração em persistência ou dependências.

## Evidências e limitações

- `dotnet build src/Api/Api.csproj`: sucesso, saída 0, 2,38 s, zero erros e
  um aviso CS9113 preexistente em CapabilityHistoryController.cs:6 (logger).
- Ambiente: Darwin arm64, SDK .NET 9.0.305, Debug/net9.0, API e suas referências.
- `git diff --check`: sucesso.
- Testes, execução HTTP/banco e geração de OpenAPI em runtime: `Not Executed`.
  A especificação não exige artefatos de teste, e execução funcional depende
  de autorização própria. Inspeção e build não comprovam execução HTTP/banco.
- Guarda documental: `Not Executed`, script ausente na baseline.
- A execução anterior com dois avisos permanece documentada no relatório 0.1;
  este build incremental reportou apenas um, sem correção dos arquivos envolvidos.

Os relatórios anteriores permanecem históricos e imutáveis. Nenhum débito
aceito, lacuna encerrada ou conclusão do workflow foi declarado.
